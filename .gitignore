*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing — NEVER commit these. Back them up securely; the same key must
# sign every Immortal release for in-place self-update to work.
keystore.properties
*.jks
*.keystore

# Built APKs / staged release artifacts and scratch screenshots.
provisioning/apks/*.apk
_chk*.png

# Local device/debug captures.
*.log
*-dump.txt

# Optional local clock fonts; verify licenses before force-adding any .ttf.
app/src/main/assets/fonts/*.ttf

# Internal strategy/planning notes — never publish.
_planning/
CHANGELOG_IMMORTAL.md

# Reverse-engineering research — never publish (repo is public). Decompilation
# working dir, APKs pulled off the device, and internal findings docs.
apk_work/
system_services.apk
*.apk
docs/
